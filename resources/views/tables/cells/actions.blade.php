{{-- <div class="flex items-center space-x-2">
    <a href="{{ route('grade.edit', $row->id) }}" class="text-blue-500 hover:text-blue-700">
        ✏️ Edit
    </a>

    <button wire:click="$emit('deleteGrade', {{ $row->id }})" class="text-red-500 hover:text-red-700">
        🗑️ Delete
    </button>
</div> --}}
